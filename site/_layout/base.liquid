<!DOCTYPE html>
<html lang="en">
  <head>
    <!--site-->
    <title>{{ title | prepend: "GM // " | default: site.title }}</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/main.css">
    <!--me-->
    <meta name="fediverse:creator" content="{{ site.author.fediverse }}">
    <link rel="me" href="{{ site.socialLinks.mastodon }}">
    <link rel="author" href="humans.txt">
  </head>
  <body>
    {% include "_include/header.liquid" %}
    <main>
      {{ content }}
    </main>
  </body>
</html>
