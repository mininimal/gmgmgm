<!DOCTYPE html>
<html lang="en">

<head>
  <!--site-->
  <title>{{ "GM // " + (title | default(site.title)) }}</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css" rel="preload">
  <link rel="canonical" href="{{ page.url }}">
  <meta name="theme-color" content="{{ site.themeColor }}">
  <!-- TODO: Link JS files with defer, like <script src="/scripts/main.js" defer></script>-->
  <!-- TODO: Favicon -->
  <!-- TODO: Social graph garbage -->
  <!--me-->
  <meta name="fediverse:creator" content="{{ site.author.fediverse }}">
  <link rel="me" href="{{ site.socialLinks.mastodon }}">
  <link rel="author" href="/humans.txt">
</head>

<body>
  {% include "header.njk" %}
  <main class="stack">
    {{ content | safe }}
  </main>
  {% include "footer.njk" %}
</body>

</html>